#include <bits/stdc++.h>

using namespace std;

vector<int> seg, nums;

int32_t main() {
    int n, k;
    
    while (cin >> n >> k) {
        seg.resize(n*4);
        nums.resize(n);

        for (int i=0; i<n; i++) {
            cin >> nums[i];
        }
    }



    return 0;
}
